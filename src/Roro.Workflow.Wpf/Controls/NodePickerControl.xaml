<UserControl x:Class="Roro.Workflow.Wpf.NodePickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Roro.Workflow.Wpf"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type={x:Type local:NodePickerControl}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            
        </Grid.Resources>
        <TreeView ItemsSource="{Binding Path=TreeViewSource}">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type local:NodePickerTreeViewItem}" ItemsSource="{Binding Path=Items}">
                    <TextBlock Text="{Binding Path=Text}" MouseLeftButtonDown="TreeView_MouseLeftButtonDown" />
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>
